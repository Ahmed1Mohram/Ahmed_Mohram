# 🌟 منصة أحمد محرم التعليمية الذهبية

منصة تعليمية متكاملة بتصميم فاخر وميزات احترافية لإدارة المحتوى التعليمي والطلاب.

## ✨ المميزات الرئيسية

### 🎓 للطلاب
- ✅ تسجيل دخول آمن برقم الهاتف
- 📚 عرض المواد الدراسية بتصميم تفاعلي
- 🎥 مشاهدة المحاضرات (فيديو، PDF، صوت)
- 📝 نظام امتحانات تفاعلي
- 💬 شات مباشر مع الدعم
- 🏆 نظام النقاط والإنجازات
- 📱 بوت تيليجرام للإشعارات

### 👨‍💼 للأدمن
- 📊 لوحة تحكم شاملة
- ✅ قبول/رفض الطلبات
- 👥 إدارة الطلاب والمستخدمين
- 📚 إدارة المواد والمحاضرات
- 📝 إدارة الامتحانات
- 💰 متابعة المدفوعات
- 🚫 نظام حظر الأجهزة
- 📸 قراءة الإيصالات تلقائياً (OCR)
- 📨 إرسال رسائل جماعية

## 🚀 البدء السريع

### 1️⃣ المتطلبات
- Node.js 18+ 
- حساب Supabase
- حساب بوت تيليجرام

### 2️⃣ التثبيت

```bash
# استنساخ المشروع
git clone [repository-url]
cd education-platform

# تثبيت الحزم
npm install
```

### 3️⃣ إعداد البيئة

1. انسخ ملف `.env.local.example` إلى `.env.local`
2. املأ جميع المتغيرات المطلوبة:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key  
SUPABASE_SERVICE_ROLE_KEY=your_service_key
TELEGRAM_BOT_TOKEN=your_bot_token
ADMIN_TELEGRAM_ID=your_telegram_id
```

### 4️⃣ إعداد قاعدة البيانات

1. افتح [Supabase Dashboard](https://app.supabase.com)
2. اذهب إلى SQL Editor
3. انسخ محتوى `supabase-schema.sql`
4. نفذه في قاعدة البيانات

### 5️⃣ تشغيل المشروع

```bash
# تشغيل الموقع
npm run dev

# تشغيل بوت التيليجرام (في نافذة منفصلة)
npm run telegram-bot
```

الموقع سيعمل على: http://localhost:3000

## 📱 إعداد بوت التيليجرام

### إنشاء البوت:
1. افتح تيليجرام وابحث عن `@BotFather`
2. أرسل `/newbot`
3. اختر اسم للبوت
4. اختر username (مثل: `ahmed_edu_bot`)
5. احفظ التوكن في `.env.local`

### الحصول على معرف التيليجرام:
1. ابحث عن `@userinfobot`
2. أرسل له رسالة
3. احفظ الـ ID في `.env.local`

## 👤 بيانات الدخول الافتراضية

### حساب الأدمن:
- **رقم الهاتف:** أحمد محرم
- **كلمة المرور:** أحمد محرم

### حساب تجريبي:
- **رقم الهاتف:** 01012345678
- **كلمة المرور:** Test@1234

## 📂 هيكل المشروع

```
education-platform/
├── app/                    # صفحات Next.js
│   ├── admin/             # لوحة الأدمن
│   ├── dashboard/         # لوحة الطالب
│   ├── subject/[id]/      # صفحة المادة
│   └── api/               # API endpoints
├── components/            # المكونات
│   ├── Chat.tsx          # نظام الشات
│   └── providers.tsx     # Context providers
├── public/               # الملفات العامة
├── telegram-bot.js       # بوت التيليجرام
└── supabase-schema.sql   # قاعدة البيانات
```

## 🔧 APIs الرئيسية

### المصادقة
- `POST /api/login-direct` - تسجيل دخول مباشر
- `POST /api/register-user` - تسجيل مستخدم جديد
- `POST /api/admin-login` - دخول الأدمن

### المواد والمحاضرات
- `GET /api/subjects` - جلب المواد
- `POST /api/subjects` - إضافة مادة
- `GET /api/subjects/[id]/lectures` - جلب المحاضرات
- `POST /api/subjects/[id]/lectures` - إضافة محاضرة

### الإدارة
- `GET /api/admin/users` - جلب المستخدمين
- `POST /api/admin/approve-user` - قبول/رفض مستخدم
- `POST /api/device-tracking` - تتبع الأجهزة
- `POST /api/ocr-receipt` - قراءة الإيصالات

## 💳 نظام الدفع

### فودافون كاش:
- **الرقم:** 01005209667
- **المبلغ:** 200 جنيه

### خطوات الاشتراك:
1. حول المبلغ على فودافون كاش
2. ارفع صورة الإيصال
3. انتظر الموافقة (تلقائية أو يدوية)

## 🚀 الرفع على الاستضافة

### Vercel:
```bash
# تثبيت Vercel CLI
npm i -g vercel

# الرفع
vercel

# أضف متغيرات البيئة في إعدادات المشروع على Vercel
```

### Railway/Render:
- ارفع الكود على GitHub
- اربط المستودع بالمنصة
- أضف متغيرات البيئة
- Deploy

## 📝 ملاحظات مهمة

1. **الأمان:**
   - لا تشارك ملف `.env.local`
   - استخدم كلمات مرور قوية
   - فعّل RLS في Supabase

2. **الأداء:**
   - استخدم CDN للصور
   - فعّل التخزين المؤقت
   - قلل حجم الصور

3. **الصيانة:**
   - خذ نسخ احتياطية دورية
   - راقب استهلاك Supabase
   - حدّث الحزم بانتظام

## 🤝 الدعم

- **واتساب:** 201005209667
- **تيليجرام:** @ahmed_moharam
- **البريد:** admin@education.com

## 📄 الرخصة

جميع الحقوق محفوظة © 2024 منصة أحمد محرم التعليمية

---

صنع بـ ❤️ بواسطة أحمد محرم